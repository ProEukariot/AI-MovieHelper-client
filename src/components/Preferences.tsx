import styled from "styled-components";

const ContentWrapper = styled.div`
  width: 100%;
  height: 100vh;
  height: 100svh;
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 4rem 0;
`;

const Container = styled.div`
  width: 100%;
  max-width: 48rem;
  height: 100%;
  padding: var(--gap-width);
`;

const Preferences = () => {
  return (
    <ContentWrapper>
      <Container>ewq</Container>
    </ContentWrapper>
  );
};

export default Preferences;
